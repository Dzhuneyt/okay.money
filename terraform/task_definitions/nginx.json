[
  {
    "links": [
      "backend",
      "frontend"
    ],
    "cpu": 256,
    "essential": true,
    "image": "${image_url}",
    "memory": 128,
    "memoryReservation": 10,
    "name": "nginx",
    "portMappings": [
      {
        "containerPort": 80,
        "hostPort": 0
      }
    ],
    "networkMode": "awsvpc",
    "logConfiguration": {
      "logDriver": "awslogs",
      "options": {
        "awslogs-group": "${log_group_name}",
        "awslogs-region": "${log_group_region}"
      }
    }
  }
]
